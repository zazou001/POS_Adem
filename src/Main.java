import pkg.connection.*;
//import pkg.dao.*;
import java.sql.Connection;

public class Main
{
    public static void main(String[] args)
    {
		// Connexion connect = new Connexion
		
		/*
		BDD adem = new BDD();
		adem.connect();
		adem.request("SELECT * FROM snack;");
		adem.disconnect();
		*/
		// adem/lbp810
		
		Login session = new Login(); 
    }
}
